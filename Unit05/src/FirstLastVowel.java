//(c) A+ Computer Science
//www.apluscompsci.com
//Name -
//Date -


public class FirstLastVowel
{
   public static String go( String a )
	{
		if (Character.toLowerCase(a.charAt(0)) == 'a' || Character.toLowerCase(a.charAt(a.length()-1)) == 'a') {
			return "yes";
		}else if (Character.toLowerCase(a.charAt(0)) == 'e' || Character.toLowerCase(a.charAt(a.length()-1)) == 'e') {
			return "yes";
		}else if (Character.toLowerCase(a.charAt(0)) == 'i' || Character.toLowerCase(a.charAt(a.length()-1)) == 'i') {
			return "yes";
		}else if (Character.toLowerCase(a.charAt(0)) == 'o' || Character.toLowerCase(a.charAt(a.length()-1)) == 'o') {
			return "yes";
		}else if (Character.toLowerCase(a.charAt(0)) == 'u' || Character.toLowerCase(a.charAt(a.length()-1)) == 'a') {
			return "yes";
		}else {
			return"no";
		
		
		
		
		
		
		
		
		
	}
}
}